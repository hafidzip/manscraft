```ts
  private laserMinerCanMine(id: number): boolean {
    if (id === B.AIR || id < 0) return false;
    if (isWaterId(id)) return false;
    if (isConveyor(id) || isInserter(id) || isLaserMiner(id) || id === B.TURRET) return false;
    if (isOreBlock(id)) return TO_FPS[id] !== undefined;
    const d = DEFS[id];
    if (!d || !isFinite(d.hardness)) return false;
+   // OPT-IN: let miners clear vegetation. OFF by default — with Option A the
+   // plant yields nothing, so a miner would burn cycles on grass while a
+   // stone column sits behind it, and any machine consumer of harvestOutcome
+   // must first be audited for `itemId === NO_ITEM`.
+   if (ALLOW_MINER_CLEAR_PLANTS && d.cross === true) return true;
    if (!d.solid) return false;
    return TO_FPS[id] !== undefined;
  }
```

**Hard prerequisite if you enable it:** every machine-side consumer of `harvestOutcome`
must handle `itemId === NO_ITEM` before inserting into a container. Inserting item `-1`
into an inventory is a much worse bug than uncuttable grass.

## 5.6 Acceptance check

1. ✅ Laser mining destroys `TALLGRASS` / `FLOWER_RED` / `FLOWER_YELLOW` in ~3 frames with
   the existing burst and `playBreak('plant')`.
2. ✅ `resetBreak()` on *every* path — harvestable, unharvestable, and failed destroy.
3. ✅ Option A (no drops) chosen and stated; Option B's exact edit list documented.
4. ✅ `removeFloatingPlantsAbove` keeps working and follows the same drop decision.
5. ✅ Automation extension supplied behind a flag, with its precondition spelled out.

---

# Cross-task integration

## Merge order (unchanged from the brief at the top): 2 → 4 → 3 → 5 → 1

Task 2 makes the light direction constant; Task 4 needs that for texel snapping and the
cached far cascade; Task 3 needs Task 4's snapping plus the constant light azimuth. Task 5
is independent. Task 1 is independent but touches the busiest region of `engine.ts`, so it
merges last to keep its diff clean.

## Files touched

| File | Tasks | Nature |
|---|---|---|
| `src/game/vehicle/spaceship.ts` | 1 | new consts/fields, 6 replaced methods, 3 new methods |
| `src/game/engine.ts` | 1, 2, 3, 4, 5 | ship setup/board/exit + persistence; shadow rig; cutout/foliage depth materials; shadow controller; mining reset |
| `src/game/engine/constants.ts` | 1, 3, 4 | `SHIP_MAX_EXIT_ALTITUDE`, cascade + grass shadow constants |
| `src/game/vfx/sky.ts` | 2 | static `SUN_DIR`/`MOON_DIR`, `sunDir()` deleted, `update()` rewritten |
| `src/game/vfx/shadowSnap.ts` | 3, 4 | **new** — texel snapping |
| `src/game/vfx/shadowLod.ts` | 4 | **new** — far cascade + receiver patch |
| `src/game/world/world.ts` | 3, 4 | `buildMesh` layers/casters, `updateGrassShadowCasters` hysteresis, `shadowGeometryVersion` |
| `src/game/world/mesher.ts` | 3 | five shadow-pass uniforms |
| `src/game/factory/machineProcessing.ts` | 5 | `NO_ITEM` + plant branch |
